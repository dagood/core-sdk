<Project>

  <Import Project="SourceBuildArcade.targets" Condition="'$(FromSource)' == 'true'" />

  <PropertyGroup>
    <GitHubRepositoryName>installer</GitHubRepositoryName>
  </PropertyGroup>

</Project>
